/**
 * Shipping Status Constants
 * 
 * Centralized shipping status strings.
 */

const ShippingStatus = {
  PENDING: "pending",
  DISPATCHED: "dispatched",
  DELIVERED: "delivered",
};

module.exports = { ShippingStatus };
